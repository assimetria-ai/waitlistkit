# ============================================================
# ENVIRONMENT VARIABLES — COPY TO .env FOR LOCAL DEVELOPMENT
# ============================================================
# SECURITY: Never commit real secrets. Use Railway environment
# variables for production. Generate unique RSA keys per product.
# Run: openssl genrsa -out private.pem 2048
#      openssl rsa -in private.pem -pubout -out public.pem
# ============================================================

NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://localhost:5432/PRODUCT_NAME_dev
DB_POOL_SSL=false
REDIS_URL=redis://localhost:6379
APP_URL=http://localhost:5173
SERVER_URL=http://localhost:4000
LOG_LEVEL=info
SERVICE_NAME=PRODUCT_NAME

# JWT RSA Keys — generate unique pair per product, load from Railway in production
# Format: single-line PEM with literal \n between lines
# NEVER share key pairs across products
JWT_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nYOUR_UNIQUE_RSA_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----
JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\nYOUR_UNIQUE_RSA_PUBLIC_KEY_HERE\n-----END PUBLIC KEY-----

# Encryption keys — UNIQUE PER PRODUCT, NEVER SHARED ACROSS PRODUCTS
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Generate IV: node -e "console.log(require('crypto').randomBytes(16).toString('base64'))"
# PRODUCTION: Set ONLY in Railway environment variables — NEVER in committed files
# SEC-2026-02-27: These keys must be rotated if ever found in git history
ENCRYPT_KEY=GENERATE_A_RANDOM_32_BYTE_BASE64_KEY
ENCRYPT_IV=GENERATE_A_RANDOM_16_BYTE_BASE64_IV
