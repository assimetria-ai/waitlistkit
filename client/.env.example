# ────────────────────────────────────────────────────────────────────────────
# Client environment variables — copy this file to .env and fill in the values.
# Never commit .env to git.
# All variables MUST be prefixed with VITE_ to be exposed to the browser.
# ────────────────────────────────────────────────────────────────────────────

# ── App ───────────────────────────────────────────────────────────────────────
VITE_APP_URL=http://localhost:5173    # Public URL of this frontend app
VITE_APP_VERSION=0.1.0               # Injected into error reports / analytics

# ── API ───────────────────────────────────────────────────────────────────────
# In development the Vite proxy rewrites /api → http://localhost:4000/api,
# so VITE_API_URL is only needed for production deploys or when running
# the client against a remote backend.
VITE_API_URL=                        # e.g. https://api.example.com/api

# ── Stripe ────────────────────────────────────────────────────────────────────
# Publishable key — safe to expose in the browser.
# Get from https://dashboard.stripe.com/test/apikeys
# Use pk_test_... in development, pk_live_... in production.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# ── Error tracking (optional) ─────────────────────────────────────────────────
VITE_ERROR_TRACKING_DSN=             # Sentry DSN
# VITE_SENTRY_DSN=                   # Alias accepted by the Sentry loader
